import angular from 'angular';
// import someDirective from './some-directive';

let directivesModule = angular.module('meanTemplate.common.directives', [])

// .filter('some-directive', someDirective)

.name;

export default directivesModule;
